# Snake-game

```bash
cd Snake-game                       # navigate into this directory
npm i                               # install dependencies
npm start                           # start electron
npm run make                        # make a offline version of the game

```
